You are continuing work on an existing repository for:

EXTREME HEATWAVE EARLY WARNING AND HUMAN THERMAL STRESS INDEX

ROLE:
Act as Principal Systems Architect, Senior Python Backend Engineer, Database Architect, Geospatial Engineer, DevOps Engineer, and Scientific Software Architect.

============================================================
MISSION
============================================================

Perform a PHASE-1 HARDENING AND CORRECTION PASS on the existing repository.

The repository already contains most of the intended Phase-1 scaffold.

DO NOT rebuild the repository.

DO NOT delete working components.

DO NOT implement the actual scientific algorithms yet.

DO NOT implement UTCI.

DO NOT implement the Hazard equation.

DO NOT implement Vulnerability scoring.

DO NOT implement Exposure scoring.

DO NOT implement HSRI calculation.

DO NOT implement machine-learning models.

DO NOT change the scientific weights or scientific thresholds.

Your purpose is to make the existing Phase-1 architecture internally consistent, reproducible, testable, and ready for the next scientific implementation phase.

============================================================
PHASE-1 DEFINITION OF DONE
============================================================

Phase 1 will be considered complete only when all of the following are true:

    Repository structure is coherent
    Backend starts
    Worker starts
    Frontend starts/builds
    PostgreSQL starts
    PostGIS works
    TimescaleDB works
    Alembic migrations work from a fresh database
    Database schema is internally consistent
    Timescale hypertables are correctly configured
    Spatial relationships are correct
    Test dependencies install correctly
    Test suite runs from a clean environment
    Existing tests pass
    No critical deprecation errors remain
    No hardcoded production secrets exist
    Documentation matches actual implementation
    Docker startup works
    No scientific algorithms are prematurely implemented

============================================================
STEP 1 — INSPECT BEFORE MODIFYING
============================================================

First inspect the complete repository.

Review:

    directory tree
    pyproject.toml
    requirements.txt
    package.json
    Dockerfiles
    docker-compose.yml
    .env.example
    Alembic configuration
    SQLAlchemy models
    migrations
    worker
    pipelines
    scientific modules
    frontend
    tests
    Kubernetes manifests
    monitoring configuration
    documentation

Identify:

    existing problems
    duplicate files
    broken imports
    missing dependencies
    database inconsistencies
    migration inconsistencies
    security issues
    architecture drift

Do not blindly overwrite files.

Preserve existing good work.

============================================================
STEP 2 — FIX PYTHON ENVIRONMENT REPRODUCIBILITY
============================================================

The current project has tests that import structlog.

Make sure every runtime dependency required by actual code is declared correctly.

Review imports across:

    apps/api
    apps/worker
    scientific
    pipelines

Ensure dependencies are declared in the correct requirements/project files.

The documented installation procedure must create an environment in which:

    pytest

can run successfully without manually installing missing packages.

Do not solve this by suppressing import errors.

Ensure the dependency graph is explicit.

Also ensure test-only dependencies are clearly separated where practical.

============================================================
STEP 3 — FIX PYDANTIC CONFIGURATION
============================================================

The project currently contains deprecated Pydantic configuration patterns.

Update them to the current supported Pydantic v2 configuration approach.

Do not change the meaning of configuration.

Verify:

    application settings load
    environment variables work
    defaults work
    validation works

Run tests afterward.

============================================================
STEP 4 — DATABASE ARCHITECTURE CORRECTION
============================================================

Review ALL SQLAlchemy models and Alembic migrations.

The database must support:

GEOGRAPHY:

    states
    cities
    wards
    grid_cells

WEATHER:

    weather_stations
    weather_observations
    weather_forecast_runs
    weather_forecasts

SCIENTIFIC REGISTRY:

    scientific_models
    model_runs

HAZARD:

    hazard_assessments

VULNERABILITY:

    vulnerability_profiles
    vulnerability_factors

EXPOSURE:

    exposure_profiles
    exposure_factors

RISK:

    risk_runs
    risk_assessments
    risk_assessment_components
    ward_risk_summaries

OPERATIONS:

    alerts
    action_recommendations

Verify:

    primary keys
    foreign keys
    nullability
    indexes
    timestamps
    relationships
    geometry types
    schema naming
    cascade behavior where appropriate

Do not add scientific formulas to database models.

============================================================
STEP 5 — TIMESCALEDB CORRECTION
============================================================

This is a critical task.

Review the current definitions for:

    weather_observations
    weather_forecasts

They are intended to be TimescaleDB hypertables.

Do not assume an ordinary PostgreSQL UUID primary key automatically works for a Timescale hypertable.

Design the keys/indexes according to actual TimescaleDB requirements.

The time-series design must support queries by:

    valid_time
    grid_cell_id
    forecast_run_id

For forecasts, preserve:

    run_time / initialization time
    valid_time
    lead time

For observations, preserve:

    observed_at

Validate the final schema against the actual TimescaleDB container.

Do not merely make SQL look correct.

Actually run the migration against TimescaleDB.

============================================================
STEP 6 — AUTHORITATIVE DATABASE LIFECYCLE
============================================================

Establish ONE clear database lifecycle.

Prefer:

    Alembic = authoritative schema evolution

The project may retain SQL scripts for:

    extension bootstrap
    database initialization
    operational SQL

but they must not create a second contradictory schema lifecycle.

A fresh database should work as:

    database starts
        ↓
    required extensions
        ↓
    Alembic upgrade head
        ↓
    complete usable schema

Verify this from a CLEAN database.

Do not rely on an existing developer database.

============================================================
STEP 7 — POSTGIS SPATIAL MODEL CORRECTION
============================================================

Review the relationship:

    grid_cells ↔ wards

Do not assume a 333m grid cell belongs to exactly one ward unless this is explicitly guaranteed by the repository's grid-generation strategy.

Prefer a robust spatial relationship model such as:

    grid_cells
        ↓
    grid_ward_intersections
        ↓
    wards

where appropriate fields may include:

    grid_cell_id
    ward_id
    intersection_area
    coverage_fraction

Use PostGIS-compatible geometry and spatial indexes.

The final design must support correct ward aggregation even when a grid cell crosses an administrative boundary.

Do not fake spatial relationships.

============================================================
STEP 8 — FORECAST TEMPORAL MODEL
============================================================

Verify the weather forecast temporal model.

It must distinguish:

    forecast/run initialization time
    valid time
    lead time

A forecast must never be represented only by one timestamp.

Ensure the schema and application types make this distinction obvious.

Add sensible validation where appropriate.

Examples:

    valid_time >= run_time

Do not over-constrain legitimate forecast workflows.

============================================================
STEP 9 — MODEL VERSIONING
============================================================

Review:

    scientific_models
    model_runs

Ensure a model run can be associated with:

    model name
    model version
    run time
    configuration/version information
    status
    timestamps

Where appropriate, support:

    configuration_hash
    algorithm version
    input snapshot/reference

Do not invent scientific metadata.

The purpose is reproducibility.

The architecture should eventually allow answering:

    "Which scientific model and configuration produced this result?"

============================================================
STEP 10 — WORKER VALIDATION
============================================================

Review:

    apps/worker/

Verify that the worker can start independently from the API.

Verify imports for all task modules:

    ingest_weather
    calculate_hazard
    calculate_risk
    aggregate_wards
    generate_alerts

and pipeline orchestration tasks.

These may remain placeholders.

Do not implement scientific calculations.

The worker must simply establish a reliable architecture for future asynchronous computation.

============================================================
STEP 11 — PIPELINE VALIDATION
============================================================

Verify these structures:

    pipelines/weather/
    pipelines/vulnerability/
    pipelines/exposure/
    pipelines/risk/

Every pipeline module should import successfully.

Each module should expose a clear placeholder boundary.

Keep responsibilities separate:

    pipelines = ingestion/transformation/orchestration

    scientific = scientific calculation

Do not mix them.

============================================================
STEP 12 — SCIENTIFIC MODULE SAFETY
============================================================

Review:

    scientific/core
    scientific/hazard
    scientific/vulnerability
    scientific/exposure
    scientific/risk
    scientific/thermal_comfort

Do not implement the actual scientific equations.

Ensure:

    imports work
    interfaces are clean
    no circular dependencies exist
    configuration can eventually be injected
    models are independently testable

The pluggable thermal-comfort architecture must remain intact.

============================================================
STEP 13 — CONFIGURATION HYGIENE
============================================================

Scientific constants must not be scattered across application code.

Preserve the configuration structure:

    scientific/configuration/

including:

    hazard_categories.yaml
    vulnerability_weights.yaml
    exposure_weights.yaml
    risk_thresholds.yaml
    model_registry.yaml

Do not change their scientific values.

Where application code currently hardcodes these values, refactor only the configuration plumbing.

Do not implement scientific computation.

============================================================
STEP 14 — SECURITY HARDENING
============================================================

Inspect:

    docker-compose.yml
    Kubernetes manifests
    .env.example
    application settings
    frontend configuration

Remove or neutralize actual production secrets.

Development placeholders are acceptable if clearly documented.

Never commit:

    real API keys
    real database passwords
    tokens
    credentials
    private certificates

Review CORS configuration.

Development may be permissive.

Production configuration must be capable of restricting allowed origins.

============================================================
STEP 15 — DOCKER HARDENING
============================================================

Inspect all Dockerfiles and docker-compose configuration.

Do not use unpinned:

    latest

tags for production-oriented infrastructure where practical.

Use reproducible versions.

Ensure the containers can actually start together.

Expected local services include:

    PostgreSQL + PostGIS + TimescaleDB
    Redis
    API
    Worker
    Web

Do not add unnecessary services.

============================================================
STEP 16 — FRONTEND VALIDATION
============================================================

The frontend should remain a scaffold.

Verify:

    Next.js starts
    TypeScript passes
    build succeeds
    existing pages compile

Do not build the full dashboard.

Do not implement scientific visualization yet.

Make sure the frontend structure remains ready for:

    Dashboard
    Risk Map
    Ward Explorer
    Forecast Explorer
    Alert Center
    Model Transparency

============================================================
STEP 17 — API VALIDATION
============================================================

Keep the API domain routes as scaffolding.

Verify:

    /api/v1/health

works correctly.

Verify the placeholder route modules import without errors:

    forecasts
    hazards
    vulnerability
    exposure
    risk
    wards
    alerts
    models

Do not implement database-backed business logic yet unless required simply to validate startup.

============================================================
STEP 18 — TEST INFRASTRUCTURE
============================================================

The project currently has basic scientific/interface tests.

Expand the test infrastructure to include smoke tests for:

    API imports
    worker imports
    database connectivity
    migration
    scientific imports
    frontend build/typecheck where practical

Create placeholders for:

    tests/end_to_end/
    tests/scientific_validation/
    tests/performance/

Do not fabricate scientific validation results.

Scientific formula tests will be implemented later.

============================================================
STEP 19 — FIX WARNINGS
============================================================

Review current warnings.

In particular, replace deprecated timezone handling such as:

    datetime.utcnow()

with proper timezone-aware UTC handling.

Use a consistent policy for timestamps across the application.

Do not mix naive and aware datetimes without an explicit reason.

============================================================
STEP 20 — DOCUMENTATION CONSISTENCY
============================================================

Compare the documentation against the implementation.

Update:

    docs/architecture/system-overview.md
    docs/architecture/data-flow.md
    docs/architecture/database.md

Ensure they describe what actually exists.

Do not document future features as already implemented.

Clearly label:

    scaffold
    implemented
    future phase

============================================================
STEP 21 — RUN A CLEAN VALIDATION
============================================================

Do not rely only on the current working environment.

Perform a clean validation as far as the repository allows.

At minimum:

    install dependencies
    start database
    enable PostGIS
    enable TimescaleDB
    run migrations from clean database
    start Redis
    start API
    start worker
    build/start frontend
    run pytest
    run lint
    run typecheck where available

If a command fails:

    diagnose the actual cause
    fix the repository
    rerun the command

Do not hide failures.

Do not mark the task complete while known critical startup/test failures remain.

============================================================
STEP 22 — NO SCIENCE IMPLEMENTATION
============================================================

Again:

DO NOT implement:

    UTCI
    Hazard equation
    Vulnerability equation
    Exposure equation
    HSRI
    risk thresholds in executable logic
    machine-learning prediction

Only establish the interfaces, configuration plumbing, database support, and testing architecture required for those later phases.

============================================================
STEP 23 — FINAL QUALITY GATE
============================================================

Before finishing, verify the following checklist:

[ ] API starts
[ ] Worker starts
[ ] Web builds/starts
[ ] PostgreSQL starts
[ ] PostGIS works
[ ] TimescaleDB works
[ ] Alembic migration succeeds from clean DB
[ ] Hypertables are actually created
[ ] Timescale constraints are satisfied
[ ] Weather time-series schema is valid
[ ] Grid/ward spatial design is valid
[ ] All domain ORM models import
[ ] All API modules import
[ ] All worker modules import
[ ] Scientific modules import
[ ] Dependencies are declared
[ ] pytest runs from documented environment
[ ] Existing tests pass
[ ] Deprecated datetime usage is removed where practical
[ ] Pydantic v2 configuration is current
[ ] No real secrets are committed
[ ] Docker configuration is reproducible
[ ] Documentation matches actual implementation
[ ] Scientific equations remain unimplemented

============================================================
FINAL REPORT FORMAT
============================================================

Return a final report containing:

1. PHASE-1 STATUS
   PASS / PASS WITH MINOR GAPS / FAIL

2. ARCHITECTURE CHANGES
   Exact files changed and why.

3. DATABASE
   Exact tables, indexes, spatial relationships, hypertables, and migrations verified.

4. INFRASTRUCTURE
   Docker/Redis/API/Worker/Web status.

5. TEST RESULTS
   Exact commands run.
   Exact result for each.

6. WARNINGS
   Remaining non-critical warnings.

7. REMAINING GAPS
   Anything still preventing Phase 1 from being considered complete.

8. NEXT PHASE
   Clearly state whether the repository is ready for the scientific-engine implementation phase.

============================================================
MOST IMPORTANT RULE
============================================================

Do not merely create files to make the repository look complete.

The repository must actually START, MIGRATE, IMPORT, BUILD, and TEST.

Verify functionality.

Do not implement the scientific algorithms during this correction pass.

Start by inspecting the repository now.