PHASE 2 SCIENTIFIC ENGINE — CORRECTION AND INTEGRATION PASS

The current scientific validation tests pass, but the implementation needs to be made architecturally correct before adding the actual UTCI calculator.

DO NOT implement mortality ML.
DO NOT implement hospitalization ML.
DO NOT implement the 3–5 day prediction model.

Do the following only:

1. Make scientific/configuration/*.yaml the single source of truth for:
   - hazard categories
   - vulnerability weights
   - exposure weights
   - nested weights
   - residual floors
   - risk thresholds

2. Create typed Pydantic configuration models for those YAML files.

3. Remove duplicated scientific constants from calculation code where the same value already exists in configuration.

4. Make the concrete Phase-2 models actually implement the Phase-1 abstract interfaces:
   - HazardModel
   - VulnerabilityModel
   - ExposureModel
   - RiskModel

   Suggested concrete implementations:
   - UTCIHazardModel / HazardNormalizationModel
   - BBWMVulnerabilityModel
   - BBWMExposureModel
   - MultiplicativeHSRIModel

5. Keep pure deterministic calculation functions underneath the model classes where useful.

6. Standardize configuration naming:
   - fluid_intake_activity
   - healthcare_accessibility

   or another single canonical vocabulary, but use it consistently in:
   YAML
   Python
   Pydantic
   tests
   database mapping

7. Make Pydantic scientific result models the canonical scientific result contract instead of having competing dataclass/Pydantic result types.

8. Preserve:
   HSRI = H × V × E

9. Preserve:
   V >= 0.33
   E >= 0.33

10. Preserve the exact supplied weights and thresholds.
    Do NOT renormalize the rounded exposure weights unless explicitly required.

11. Add tests verifying:
    - configuration loads correctly
    - configuration values match the expected scientific specification
    - interface implementations work
    - V minimum/maximum
    - E minimum/maximum
    - HSRI minimum/maximum
    - risk boundaries
    - deterministic output
    - invalid scores rejected
    - no unknown Pydantic fields accepted

12. Do NOT implement the actual UTCI formula yet unless an authoritative/reference implementation and validation data are available in the repository.

13. The next planned component after this pass is:
       actual UTCI calculator
    accepting:
       air temperature
       relative humidity
       wind speed
       mean radiant temperature

14. Update documentation to distinguish:
    - implemented normalized scoring engine
    - not-yet-implemented UTCI calculation
    - later mortality ML

15. Run:
    pytest -q
    ruff check .
    mypy ...

Do not claim scientific validation merely because unit tests pass.

Final report must state:
    what was changed
    what scientific constants are now configuration-driven
    which Phase-1 interfaces are concretely implemented
    tests passed
    remaining scientific gaps