You are continuing an existing project.

PROJECT:
Extreme Heatwave Early Warning and Human Thermal Stress Index

ROLE:
Act as Principal Systems Architect, Senior Backend Engineer, Geospatial Engineer, Data Engineer, and Scientific Software Architect.

IMPORTANT:
The repository has already been partially scaffolded.

DO NOT rebuild the project from scratch.
DO NOT delete or unnecessarily rewrite existing working files.
DO NOT implement the actual UTCI, Vulnerability, Exposure, or HSRI scientific algorithms yet.

Your task is to COMPLETE THE REMAINING PHASE-1 ARCHITECTURAL SCAFFOLDING.

============================================================
CURRENT STATE
============================================================

The repository already contains the basic:

- FastAPI backend
- Next.js frontend
- PostgreSQL
- PostGIS
- TimescaleDB
- Redis configuration
- scientific module interfaces
- scientific configuration YAML files
- basic health endpoints
- basic frontend
- basic scientific interface tests

Inspect the repository before changing anything.

Reuse existing conventions where they are sound.

============================================================
PRIMARY OBJECTIVE
============================================================

Complete the missing architectural skeleton so the repository is ready for implementation of the scientific engine in the next phase.

The target architecture is:

    apps/
        api/
        worker/
        web/

    scientific/
        core/
        hazard/
        vulnerability/
        exposure/
        risk/
        thermal_comfort/
        configuration/

    pipelines/
        weather/
        vulnerability/
        exposure/
        risk/

    db/
        migrations/
        seeds/
        sql/
        fixtures/

    data/
        raw/
        staging/
        processed/

    infra/
        docker/
        kubernetes/
        monitoring/

    scripts/

    docs/
        architecture/
        scientific/
        operations/

    tests/
        end_to_end/
        scientific_validation/
        performance/

============================================================
TASK 1 — INSPECT THE REPOSITORY
============================================================

Before modifying files:

1. Inspect the complete repository tree.
2. Identify what already exists.
3. Identify missing architectural components.
4. Identify naming inconsistencies.
5. Identify duplicate abstractions.
6. Identify existing tests.
7. Identify current database configuration.
8. Identify current Docker configuration.
9. Identify package/dependency configuration.

Do not guess.

Do not destroy existing work.

After inspection, create a concise implementation plan and then execute it.

============================================================
TASK 2 — COMPLETE THE BACKEND STRUCTURE
============================================================

Make sure the FastAPI application has clear separation between:

    api/
    schemas/
    services/
    db/
    core/

Create missing route modules as skeletons where appropriate:

    forecasts.py
    hazards.py
    vulnerability.py
    exposure.py
    risk.py
    wards.py
    alerts.py
    models.py

Register them through:

    /api/v1/

These routes may return placeholder responses or "not implemented" responses.

DO NOT implement scientific calculations in route handlers.

Every endpoint must have a clear future responsibility.

============================================================
TASK 3 — DATABASE DOMAIN MODELS
============================================================

Create SQLAlchemy models and relationships for the following concepts.

GEOGRAPHY:

    states
    cities
    wards
    grid_cells

WEATHER:

    weather_stations
    weather_observations
    weather_forecast_runs
    weather_forecasts

SCIENTIFIC MODEL REGISTRY:

    scientific_models
    model_runs

HAZARD:

    hazard_assessments

VULNERABILITY:

    vulnerability_profiles
    vulnerability_factors

EXPOSURE:

    exposure_profiles
    exposure_factors

RISK:

    risk_runs
    risk_assessments
    risk_assessment_components
    ward_risk_summaries

OPERATIONS:

    alerts
    action_recommendations

Use explicit foreign keys and relationships.

Use UUIDs where appropriate.

Use timezone-aware timestamps.

Use PostGIS geometry columns where appropriate.

Use appropriate spatial indexes.

Do not put scientific formulas into ORM models.

============================================================
TASK 4 — SPATIAL DATABASE DESIGN
============================================================

Use PostGIS as the spatial system.

Expected spatial hierarchy:

    state
      ↓
    city
      ↓
    ward
      ↓
    grid_cell

The computational grid is approximately:

    333 metres

Do not assume every grid cell belongs exclusively to exactly one ward if that would make the spatial model scientifically inaccurate.

Where appropriate, support spatial relationships or future coverage fractions.

Create:

    db/sql/extensions.sql
    db/sql/indexes.sql
    db/sql/hypertables.sql

Enable:

    PostGIS
    TimescaleDB

Do not duplicate extension creation across random application files.

============================================================
TASK 5 — TIMESCALEDB DESIGN
============================================================

Prepare the architecture for high-volume weather time series.

The weather forecast model must distinguish:

    forecast run / initialization time
    valid time
    lead time

Create the appropriate database structure so that:

    weather_forecasts

can become a TimescaleDB hypertable.

Choose indexes carefully around:

    valid_time
    grid_cell_id
    run_id

Do not create destructive database changes.

Do not use "latest" image tags in production configuration if you encounter them.

Pin infrastructure versions where practical.

============================================================
TASK 6 — ALEMBIC
============================================================

Complete Alembic configuration.

Create the initial migration for the current domain schema.

The migration should establish:

    tables
    foreign keys
    indexes
    geometry columns
    required extensions where appropriate
    TimescaleDB preparation where appropriate

Do not create fake scientific data.

Create seed scripts separately.

Verify:

    fresh database
        ↓
    migration
        ↓
    application startup

works successfully.

============================================================
TASK 7 — WORKER APPLICATION
============================================================

Create:

    apps/worker/

It should be a separate Python application/process.

Create task/job skeletons for:

    ingest_weather
    calculate_hazard
    calculate_risk
    aggregate_wards
    generate_alerts

Also create pipeline orchestration skeletons:

    forecast_pipeline
    risk_pipeline

These are orchestration placeholders only.

DO NOT implement scientific equations yet.

The architectural principle is:

    HTTP request
        ≠
    long-running scientific computation

============================================================
TASK 8 — PIPELINE DIRECTORIES
============================================================

Create:

    pipelines/weather/
    pipelines/vulnerability/
    pipelines/exposure/
    pipelines/risk/

Add placeholder modules such as:

WEATHER:

    ingest.py
    normalize.py
    quality_control.py
    spatialize.py

VULNERABILITY:

    ingest.py
    anonymize.py
    aggregate.py

EXPOSURE:

    ingest.py
    normalize.py
    spatialize.py

RISK:

    run.py
    validate.py
    publish.py

Each module should contain a clean function/class boundary for future implementation.

Do not put full scientific logic here.

Pipelines move and transform data.

Scientific modules calculate scientific results.

============================================================
TASK 9 — SCIENTIFIC MODULE BOUNDARIES
============================================================

Do not implement the equations yet.

Make sure the repository contains clean boundaries for:

    scientific/hazard/
    scientific/vulnerability/
    scientific/exposure/
    scientific/risk/
    scientific/thermal_comfort/

Ensure there are no circular dependencies.

Scientific modules must not import FastAPI route handlers.

Scientific modules must not depend directly on frontend code.

Scientific calculations should eventually be deterministic and independently testable.

============================================================
TASK 10 — PLUGGABLE THERMAL COMFORT
============================================================

Complete:

    scientific/thermal_comfort/

Create a clean interface/abstract contract.

It must support future implementations such as:

    UTCI
    Adaptive Thermal Comfort
    Future thermal models

Create:

    base.py
    registry.py

and suitable model placeholders.

Do not implement an alternative comfort model merely for demonstration.

Do not invent formulas.

============================================================
TASK 11 — MODEL REGISTRY
============================================================

The project already contains YAML scientific configuration.

Preserve that.

Create an architectural bridge between:

    YAML/configuration
          ↓
    scientific model registry
          ↓
    model run
          ↓
    risk assessment

The system must eventually be able to answer:

    Which scientific model/version produced this result?

Create the persistence skeleton for:

    scientific_models
    model_runs

Do not invent scientific model versions beyond what is needed as placeholders.

============================================================
TASK 12 — API SCHEMAS
============================================================

Create Pydantic schemas for:

    weather
    hazard
    vulnerability
    exposure
    risk
    wards
    alerts
    model metadata

Separate:

    database entities

from:

    API request/response schemas

Do not expose database models directly from API routes.

============================================================
TASK 13 — FRONTEND STRUCTURE
============================================================

Do not build the complete dashboard yet.

Create the architectural pages/components for:

    Dashboard
    Risk Map
    Ward Explorer
    Forecast Explorer
    Alert Center
    Model / Scientific Transparency

Set up clear component directories:

    components/map/
    components/risk/
    components/weather/
    components/charts/
    components/wards/
    components/common/

Set up:

    TanStack Query
    API client
    TypeScript domain types
    Zod validation

MapLibre can be initialized at the infrastructure/component level, but do not implement the full production map yet.

============================================================
TASK 14 — DATA DIRECTORY
============================================================

Create:

    data/raw/
    data/staging/
    data/processed/

Add README files explaining that these are development/data-pipeline locations.

Do not commit sensitive datasets.

Do not generate fake personal medical records.

============================================================
TASK 15 — INFRASTRUCTURE
============================================================

Complete the infrastructure skeleton:

    infra/docker/
    infra/kubernetes/
    infra/monitoring/

Create placeholder Kubernetes manifests for:

    namespace
    api
    worker
    web
    ingress

Do not claim production readiness.

Create monitoring placeholders for:

    Prometheus
    Grafana

Create appropriate Dockerfiles for:

    API
    worker
    web

Pin versions where practical.

============================================================
TASK 16 — OBSERVABILITY
============================================================

Create a basic observability architecture.

At minimum support:

    structured logs
    request correlation
    application health
    worker health
    database health

Prepare placeholders/configuration for:

    Prometheus
    Grafana
    OpenTelemetry

Do not introduce unnecessary complexity.

============================================================
TASK 17 — SECURITY / CONFIGURATION
============================================================

Review environment configuration.

Create/update:

    .env.example

Never commit:

    passwords
    API keys
    secrets
    tokens

Use configuration classes rather than scattered environment-variable reads.

Use separate configuration concerns for:

    database
    Redis
    application
    external data sources
    security

============================================================
TASK 18 — TESTING
============================================================

Expand testing infrastructure.

Create:

    tests/end_to_end/
    tests/scientific_validation/
    tests/performance/

Add smoke tests for:

    API startup
    health endpoint
    database connectivity
    migrations
    worker import/startup
    frontend build
    scientific module imports

DO NOT pretend scientific validation is complete.

Scientific formula tests will be added in a later phase.

Fix existing Python deprecation warnings where practical, especially timezone handling.

============================================================
TASK 19 — DOCUMENTATION
============================================================

Create:

    docs/architecture/system-overview.md
    docs/architecture/data-flow.md
    docs/architecture/database.md

Also create:

    docs/scientific/hazard.md
    docs/scientific/vulnerability.md
    docs/scientific/exposure.md
    docs/scientific/risk.md

And:

    docs/operations/deployment.md
    docs/operations/monitoring.md
    docs/operations/incident-response.md

Document architecture only.

Do not invent scientific results.

============================================================
TASK 20 — ROOT PROJECT FILES
============================================================

Ensure the root has appropriate:

    README.md
    .env.example
    .gitignore
    .editorconfig
    pyproject.toml
    package.json
    pnpm-workspace.yaml
    docker-compose.yml
    Makefile

Do not unnecessarily replace working project metadata.

============================================================
TASK 21 — DEPENDENCY HYGIENE
============================================================

Review backend and frontend dependencies.

Remove unnecessary duplicates.

Do not blindly install libraries just because they appear in the original blueprint.

Only add dependencies that are actually needed for the current scaffold.

Keep scientific/geospatial dependencies isolated and documented.

============================================================
TASK 22 — DATABASE DESIGN RULES
============================================================

Follow these rules:

- Geometry is first-class.
- Time is first-class.
- Forecast run time and valid time are distinct.
- Scientific model version is first-class.
- Risk assessment is traceable.
- Sensitive vulnerability data is not exposed through public endpoints.
- High-volume time-series data is designed for TimescaleDB.
- Spatial queries are designed for PostGIS.

============================================================
TASK 23 — DO NOT IMPLEMENT YET
============================================================

DO NOT implement:

    actual UTCI algorithm
    actual Hazard calculation
    actual vulnerability scoring
    actual exposure scoring
    actual HSRI calculation
    operational alert thresholds beyond configuration scaffolding
    machine-learning prediction models
    AI-generated scientific conclusions

Those belong to later implementation phases.

============================================================
TASK 24 — QUALITY GATE
============================================================

Before declaring this task complete:

Run the available checks.

At minimum attempt:

    backend tests
    frontend typecheck/build
    lint
    database migration
    application startup

Verify:

    PostgreSQL starts
    PostGIS is enabled
    TimescaleDB is enabled
    migrations succeed
    API starts
    worker starts/imports
    frontend starts/builds
    baseline tests pass

If something fails:

    diagnose
    fix
    rerun

Do not simply report the failure without attempting a reasonable correction.

============================================================
TASK 25 — FINAL REPORT
============================================================

At the end, report:

1. Files created
2. Files modified
3. Files intentionally left untouched
4. Database tables created
5. Migrations created
6. Worker tasks created
7. Pipeline modules created
8. API routes created
9. Frontend pages/components created
10. Tests added
11. Commands executed
12. Test/build results
13. Remaining Phase-1 gaps
14. Recommended next phase

Use exact repository paths.

Do not claim anything is complete unless it actually exists and was verified.

============================================================
MOST IMPORTANT RULE
============================================================

This is an existing repository.

Inspect first.

Preserve good existing work.

Implement only the missing Phase-1 architecture.

Do not jump ahead into scientific algorithm implementation.

The goal of this task is to leave the repository in a clean state where Phase 2 can begin with:

    DATABASE
        ↓
    DATA PIPELINES
        ↓
    SCIENTIFIC ENGINE
        ↓
    RISK ENGINE
        ↓
    API
        ↓
    FRONTEND

Start now by inspecting the repository.