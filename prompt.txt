FINAL PHASE-1 REPOSITORY CONSISTENCY CHECK

Inspect the current repository exactly as committed.

Do NOT implement Phase 2.
Do NOT implement UTCI, WBGT, Hazard, Vulnerability, Exposure, HSRI, mortality ML, hospitalization ML, or any scientific algorithm.

Do NOT redesign the architecture.

I found that docker-compose.yml references:

    infra/docker/Dockerfile.db

but this file appears to be missing from the repository.

Resolve this inconsistency.

Requirements:

1. Ensure docker-compose.yml references a database Dockerfile that actually exists in the repository.

2. The database container must provide:
   - PostgreSQL
   - PostGIS
   - TimescaleDB

3. Keep the current PostgreSQL + PostGIS + TimescaleDB architecture.

4. Do not introduce Supabase, Firebase, AWS, MongoDB, Kafka, or any unrelated platform.

5. Start from a completely clean database volume.

6. Build the Docker images from the repository itself.

7. Start the database.

8. Verify:
   - PostgreSQL works
   - PostGIS extension exists
   - TimescaleDB extension exists

9. Run:

   cd apps/api
   alembic upgrade head

10. Verify from the actual running database:
    - all expected tables exist
    - weather_observations is a hypertable
    - weather_forecasts is a hypertable
    - TimescaleDB constraints are valid
    - spatial indexes exist
    - grid_ward_intersections exists
    - coverage_fraction exists
    - intersection_area exists

11. Start:
    - Redis
    - API
    - Worker
    - Web

12. Verify:
    - API health returns 200
    - worker imports/starts
    - frontend typecheck passes
    - frontend build passes
    - frontend lint passes

13. Run:

    pytest -q

14. Run:

    ruff check apps/ scientific/ pipelines/ tests/

15. Do not modify scientific constants.

16. Do not modify scientific model implementations because they are intentionally Phase-2 work.

17. Make sure README claims match what was actually verified.

IMPORTANT:
The final repository must be self-contained.
Every file referenced by Docker Compose, scripts, Makefile, migrations, or documentation must actually exist.

FINAL REPORT:

Provide:

File consistency: PASS/FAIL
Docker build: PASS/FAIL
PostgreSQL: PASS/FAIL
PostGIS: PASS/FAIL
TimescaleDB: PASS/FAIL
Clean Alembic migration: PASS/FAIL
Hypertables: PASS/FAIL
Spatial model: PASS/FAIL
Redis: PASS/FAIL
API: PASS/FAIL
Worker: PASS/FAIL
Frontend build: PASS/FAIL
Frontend typecheck: PASS/FAIL
Frontend lint: PASS/FAIL
pytest: PASS/FAIL
Ruff: PASS/FAIL

List exact files changed.

Only state:

PHASE 1 COMPLETE — READY FOR PHASE 2

if all critical checks genuinely pass from the repository itself.