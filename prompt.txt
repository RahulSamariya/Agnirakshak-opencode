Run the final Phase-1 integration acceptance test.

Do not implement Phase 2.
Do not implement UTCI, WBGT, H, V, E, HSRI, mortality ML, hospitalization ML, or any scientific algorithm.

Do not redesign the architecture.

Perform these checks against the ACTUAL repository:

1. Create a completely clean Python environment.
2. Install the documented Python dependencies.
3. Run pytest.
4. Start a completely fresh PostgreSQL + PostGIS + TimescaleDB database.
5. Do NOT manually create application tables.
6. Run:

   cd apps/api
   alembic upgrade head

7. Verify from the running database:

   - PostGIS extension exists
   - TimescaleDB extension exists
   - all expected domain tables exist
   - weather_observations is a hypertable
   - weather_forecasts is a hypertable
   - TimescaleDB primary-key/unique-index constraints are valid
   - spatial indexes exist
   - grid_ward_intersections exists
   - coverage_fraction exists
   - intersection_area exists

8. Start Redis.
9. Start the API.
10. Verify /api/v1/health.
11. Start/import the worker.
12. Verify scientific packages import.
13. Run frontend:

   pnpm install
   pnpm --filter heatwave-web type-check
   pnpm --filter heatwave-web build
   pnpm --filter heatwave-web lint

14. Run:

   ruff check apps/ scientific/ pipelines/ tests/

15. Do not modify scientific constants.
16. Do not implement Phase 2.

IMPORTANT:

Do not report PASS based on static code inspection.

Actually connect to the database and verify TimescaleDB/PostGIS.

At the end return:

CHECK                         RESULT
Python install                PASS/FAIL
pytest                        PASS/FAIL
PostgreSQL                    PASS/FAIL
PostGIS                       PASS/FAIL
TimescaleDB                   PASS/FAIL
Alembic clean migration       PASS/FAIL
Weather hypertables           PASS/FAIL
Spatial model                 PASS/FAIL
API                           PASS/FAIL
Worker                        PASS/FAIL
Frontend typecheck            PASS/FAIL
Frontend build                PASS/FAIL
Frontend lint                 PASS/FAIL
Ruff                          PASS/FAIL

Then list every failure and its root cause.

Only if all critical checks pass, state:

PHASE 1 COMPLETE — READY FOR PHASE 2.

Otherwise state:

PHASE 1 NOT COMPLETE.