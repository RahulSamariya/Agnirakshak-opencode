You are the Principal Systems Architect responsible for completing Phase 1 of the:

EXTREME HEATWAVE EARLY WARNING AND HUMAN THERMAL STRESS INDEX

PROJECT.

The repository has already been substantially scaffolded.

Your task now is to perform the FINAL PHASE-1 COMPLETION, HARDENING, AND VERIFICATION PASS.

============================================================
CRITICAL SCOPE RULE
============================================================

DO NOT rebuild the project.

DO NOT replace the existing architecture.

DO NOT implement Phase 2.

DO NOT implement:

- UTCI calculation
- WBGT calculation
- Heat Index calculation
- Hazard Index H calculation
- Vulnerability calculation
- Exposure calculation
- HSRI calculation
- Mortality prediction
- Hospitalization prediction
- Machine learning models
- ML training
- Real weather-provider integration
- SMS integration
- WhatsApp integration
- Production alert algorithms
- Full GIS analytics

Those belong to later phases.

Your ONLY objective is to make Phase 1 technically complete, reproducible, and verified.

============================================================
PHASE-1 DEFINITION
============================================================

Phase 1 establishes:

1. Repository architecture
2. Backend foundation
3. Frontend foundation
4. PostgreSQL
5. PostGIS
6. TimescaleDB
7. Redis/Celery worker foundation
8. Database schema
9. Alembic migration system
10. Spatial data architecture
11. Weather time-series architecture
12. Scientific module interfaces
13. Scientific configuration management
14. API contracts
15. Pipeline contracts
16. Testing infrastructure
17. Docker development environment
18. Basic Kubernetes/monitoring scaffolding
19. Documentation
20. Reproducible development workflow

The scientific algorithms themselves remain Phase 2.

============================================================
STEP 1 — INSPECT CURRENT REPOSITORY
============================================================

Before making changes:

- inspect the complete repository
- inspect current git/status if available
- inspect directory tree
- inspect Python configuration
- inspect frontend configuration
- inspect Docker Compose
- inspect Dockerfiles
- inspect SQLAlchemy models
- inspect Alembic configuration
- inspect all migrations
- inspect PostGIS SQL
- inspect TimescaleDB SQL
- inspect worker configuration
- inspect pipeline modules
- inspect scientific interfaces
- inspect configuration YAML
- inspect tests
- inspect documentation

DO NOT recreate files that already work.

Identify only the remaining Phase-1 gaps.

============================================================
STEP 2 — PYTHON DEPENDENCY REPRODUCIBILITY
============================================================

Make the Python environment reproducible.

Inspect all Python imports used by:

    apps/api
    apps/worker
    scientific
    pipelines
    tests

Ensure every required runtime/test dependency is declared correctly.

The important requirement is:

A fresh environment must be able to install the declared dependencies and execute the test suite.

Do NOT solve missing dependencies by adding import fallbacks.

Do NOT hide import errors.

Make the documented installation workflow actually work.

Prefer the project's existing dependency-management approach rather than introducing unnecessary package managers.

============================================================
STEP 3 — PYDANTIC / PYTHON CLEANUP
============================================================

Review configuration code.

Ensure Pydantic v2 configuration is used correctly.

Remove deprecated configuration patterns.

Review datetime usage.

Do not use naive UTC timestamps where timezone-aware UTC timestamps are required.

Replace deprecated patterns such as:

    datetime.utcnow()

with appropriate timezone-aware UTC handling.

Do not change business semantics.

Run tests after changes.

============================================================
STEP 4 — DATABASE MIGRATION LIFECYCLE
============================================================

This is a critical requirement.

Establish:

    Alembic = authoritative schema migration mechanism

The repository may retain SQL files for:

    extension bootstrap
    Timescale-specific operations
    indexes
    operational database tasks

but there must not be two contradictory schema initialization mechanisms.

The target lifecycle is:

    fresh PostgreSQL/TimescaleDB database
                ↓
         required extensions
                ↓
        alembic upgrade head
                ↓
          complete schema
                ↓
       Timescale hypertables
                ↓
             indexes
                ↓
             READY

Determine the cleanest implementation using the current repository.

Do not blindly delete init.sql or other SQL files.

Instead, make their responsibilities explicit.

============================================================
STEP 5 — FRESH DATABASE TEST
============================================================

Do not validate against an existing developer database.

Create or recreate a clean development database/volume.

Run:

    docker compose up -d

or the repository's documented equivalent.

Then execute the migration from an empty database:

    alembic upgrade head

Verify that migration succeeds without manual database modifications.

If migration fails:

    diagnose the real error
    fix the migration/schema
    destroy/reset the test database
    rerun migration

Do not mark the task complete until this works.

============================================================
STEP 6 — POSTGIS VERIFICATION
============================================================

Verify that PostGIS is actually enabled.

Verify spatial columns for:

    states
    cities
    wards
    grid_cells
    grid_ward_intersections

where applicable.

Verify appropriate spatial indexes exist.

Do not merely inspect migration source.

Actually query the running database.

Record the verification result in the final report.

============================================================
STEP 7 — TIMESCALEDB VERIFICATION
============================================================

This is mandatory.

Verify TimescaleDB is actually enabled.

Verify that the intended time-series tables are valid TimescaleDB hypertables.

Expected candidates include:

    weather_observations
    weather_forecasts

Use the running database to verify the hypertables.

Check:

    timescaledb_information.hypertables

or the appropriate TimescaleDB metadata view for the installed version.

Verify that the schema's primary keys and unique indexes comply with actual TimescaleDB requirements.

Do not assume ordinary PostgreSQL constraints automatically work on hypertables.

If necessary, redesign the key/index strategy while preserving the logical data model.

Then rerun the migration from a completely clean database.

============================================================
STEP 8 — WEATHER TEMPORAL MODEL
============================================================

Verify the forecast schema clearly distinguishes:

    forecast run / initialization time
    valid time
    lead time

The logical relationship should be clear.

Where appropriate, validate:

    valid_time >= run_time

Do not over-constrain legitimate forecast workflows.

Verify observations separately use their observation timestamp.

Do not implement actual weather ingestion.

============================================================
STEP 9 — SPATIAL GRID/WARD MODEL
============================================================

Verify the existing spatial architecture:

    grid_cells
          ↓
    grid_ward_intersections
          ↓
        wards

The intersection table should support:

    grid_cell_id
    ward_id
    intersection_area
    coverage_fraction

where appropriate.

Ensure this architecture supports a grid cell intersecting multiple wards.

Verify foreign keys.

Verify spatial indexes.

Verify that future ward aggregation can use coverage_fraction/intersection geometry.

Do not implement actual aggregation algorithms yet.

============================================================
STEP 10 — SCIENTIFIC MODEL REGISTRY
============================================================

Verify:

    scientific_models
    model_runs

exist and have sensible relationships.

The architecture must eventually allow us to determine:

    model
    model version
    configuration/version
    run
    run timestamp
    status

Do not invent scientific model results.

Do not implement UTCI or HSRI.

This is only model-run traceability infrastructure.

============================================================
STEP 11 — WORKER VERIFICATION
============================================================

Verify:

    apps/worker/

can import/start independently.

Verify the task modules:

    ingest_weather
    calculate_hazard
    calculate_risk
    aggregate_wards
    generate_alerts

can be imported.

They may remain placeholders.

Do NOT implement the scientific computations inside them.

Verify Redis/Celery configuration is valid for local development.

============================================================
STEP 12 — PIPELINE VERIFICATION
============================================================

Verify these modules exist and import successfully:

    pipelines/weather/
    pipelines/vulnerability/
    pipelines/exposure/
    pipelines/risk/

Keep the distinction:

    pipeline = ingestion/transformation/orchestration

    scientific = scientific calculation

Do not move scientific logic into pipelines.

============================================================
STEP 13 — API VERIFICATION
============================================================

Verify:

    /api/v1/health

works.

Verify the domain route modules import correctly:

    forecasts
    hazards
    vulnerability
    exposure
    risk
    wards
    alerts
    models

Placeholder responses are acceptable.

Do not implement business/scientific calculations.

Verify OpenAPI generation works if configured.

============================================================
STEP 14 — FRONTEND VERIFICATION
============================================================

Verify the Next.js application:

    installs
    typechecks
    builds
    starts

Verify the existing pages/routes.

Do not build the production dashboard.

Do not implement real risk visualization yet.

Do not invent data.

============================================================
STEP 15 — SECURITY CHECK
============================================================

Search the repository for:

    passwords
    API keys
    tokens
    credentials
    private keys
    connection strings containing real secrets

Remove any real secret.

Development placeholders such as:

    CHANGE_ME

are acceptable.

Ensure .env.example contains examples only.

Ensure production Kubernetes configuration expects secrets to be injected rather than committed.

Review CORS.

Development can remain permissive if clearly documented.

Do not over-engineer production security in this phase.

============================================================
STEP 16 — DOCKER VALIDATION
============================================================

Review:

    docker-compose.yml
    API Dockerfile
    worker Dockerfile
    web Dockerfile

Verify image versions are reproducible.

Do not use:

    latest

for infrastructure where practical.

Verify the services start together.

Expected local infrastructure:

    PostgreSQL + PostGIS + TimescaleDB
    Redis
    API
    Worker
    Web

Do not add unnecessary services.

============================================================
STEP 17 — TEST SUITE
============================================================

Make sure the repository's tests actually run.

At minimum test:

    API imports
    API health
    worker imports
    scientific imports
    database configuration
    migration
    basic integration paths

Create/maintain:

    tests/scientific_validation/
    tests/integration/
    tests/end_to_end/
    tests/performance/

Do not add fake scientific validation.

Phase-2 scientific tests will come later.

The final command should be reproducible, for example:

    pytest -q

or the repository's documented equivalent.

It must actually execute successfully in the clean environment.

============================================================
STEP 18 — LINT / TYPE CHECK
============================================================

Run the project's configured:

    Ruff
    mypy
    TypeScript typecheck
    frontend lint

Do not introduce a new linting framework unnecessarily.

Fix genuine errors.

Do not suppress errors globally just to obtain a green result.

If an existing check is intentionally unavailable, document why.

============================================================
STEP 19 — DOCUMENT THE ACTUAL SYSTEM
============================================================

Update documentation only where necessary.

Ensure:

    docs/architecture/system-overview.md
    docs/architecture/data-flow.md
    docs/architecture/database.md

describe what actually exists.

Clearly distinguish:

    IMPLEMENTED IN PHASE 1

from:

    PLANNED FOR PHASE 2+

Do not claim that UTCI, HSRI, mortality prediction, or ML are implemented.

If the README currently says scientific models are implemented when they are only interfaces/configuration, correct that wording.

============================================================
STEP 20 — FINAL CLEAN VALIDATION
============================================================

Perform a complete clean validation.

Preferred order:

    1. Clean Python environment
    2. Install declared dependencies
    3. Clean database
    4. Start infrastructure
    5. Run Alembic migration
    6. Verify PostGIS
    7. Verify TimescaleDB
    8. Verify hypertables
    9. Start API
    10. Start worker
    11. Build/typecheck frontend
    12. Run tests
    13. Run lint
    14. Run type checks

Do not rely only on static inspection.

Actually execute the important checks.

============================================================
PHASE-1 FINAL CHECKLIST
============================================================

Before declaring Phase 1 complete:

[ ] Repository structure correct
[ ] FastAPI starts
[ ] API health works
[ ] Worker starts/imports
[ ] Redis works
[ ] Next.js starts/builds
[ ] PostgreSQL works
[ ] PostGIS works
[ ] TimescaleDB works
[ ] Alembic works from a clean database
[ ] All domain tables exist
[ ] Hypertables actually exist
[ ] Timescale constraints are valid
[ ] Spatial indexes exist
[ ] Grid/ward intersection model works
[ ] Forecast temporal model is correct
[ ] Scientific model registry exists
[ ] Scientific interfaces import
[ ] Pipeline modules import
[ ] API modules import
[ ] Dependencies install cleanly
[ ] pytest runs
[ ] pytest passes
[ ] lint passes
[ ] typecheck passes where configured
[ ] no real secrets committed
[ ] Docker is reproducible
[ ] documentation matches implementation
[ ] no Phase-2 scientific algorithms were added

============================================================
IMPORTANT: DO NOT CHEAT THE QUALITY GATE
============================================================

Do NOT:

- mark a test as skipped merely to obtain PASS
- disable failing tests
- suppress import errors
- remove tests that fail
- mock the database when validating migrations
- claim TimescaleDB works without connecting to TimescaleDB
- claim PostGIS works without querying the database
- claim Docker works without starting it
- claim the worker works without importing/starting it
- claim the frontend builds without running the build
- silently modify scientific constants
- implement Phase 2 because a placeholder is inconvenient

If something genuinely cannot be verified in the current environment, report it explicitly.

============================================================
FINAL REPORT
============================================================

At completion, return:

# PHASE 1 FINAL STATUS

Choose exactly one:

    READY FOR PHASE 2
    NOT READY FOR PHASE 2

Then provide:

## 1. Repository changes

List exact files created/modified.

## 2. Database verification

Report:

    migration result
    tables
    foreign keys
    spatial indexes
    hypertables
    TimescaleDB verification
    PostGIS verification

## 3. Runtime verification

Report:

    PostgreSQL
    Redis
    API
    Worker
    Web

## 4. Quality verification

Report exact commands and results:

    pytest
    lint
    typecheck
    frontend build

## 5. Security verification

Report whether real secrets were found.

## 6. Remaining Phase-1 gaps

Be explicit.

## 7. Phase-2 readiness

If and ONLY if all critical checks pass, state:

    PHASE 1 IS COMPLETE.
    THE REPOSITORY IS READY FOR PHASE 2 SCIENTIFIC ENGINE IMPLEMENTATION.

Otherwise state:

    PHASE 1 IS NOT COMPLETE.

and list the exact blockers.

============================================================
FINAL ARCHITECTURAL RULE
============================================================

The final Phase-1 architecture must provide this foundation:

    DATA SOURCES
         ↓
    DATA/PERSISTENCE
    PostgreSQL
    PostGIS
    TimescaleDB
         ↓
    PIPELINES
         ↓
    WORKER
         ↓
    SCIENTIFIC INTERFACES
         ↓
    API
         ↓
    FRONTEND

Phase 2 will then implement:

    Thermal Comfort / UTCI
          ↓
    Hazard H
          ↓
    Vulnerability V
          ↓
    Exposure E
          ↓
    HSRI = H × V × E

Later phases will implement:

    Historical Health Dataset
          ↓
    Mortality / Hospitalization ML
          ↓
    3–5 Day Health Impact Prediction
          ↓
    Early Warning / GIS / Alerts

Do not cross these phase boundaries.

START NOW BY INSPECTING THE EXISTING REPOSITORY.