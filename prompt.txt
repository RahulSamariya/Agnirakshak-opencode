We are continuing Phase 2.

Do NOT start Phase 3.
Do NOT implement mortality ML.
Do NOT implement hospitalization ML.
Do NOT redesign the architecture.

Perform the following in order:

1. Fix the existing hazard-category bug where H-score values
   are incorrectly compared against UTCI bounds.

2. Add semantic tests proving that H values map to the correct
   hazard categories.

3. Remove remaining duplicated scientific constants where the
   same values already exist in scientific/configuration.

4. Tighten configuration validation.

5. Keep Pydantic scientific contracts canonical.

6. Update documentation/test counts.

7. Then inspect the actual UTCI methodology and reference data
   available in the repository.

8. If an authoritative/reference UTCI algorithm and reference
   values are available:
       implement scientific/thermal_comfort/utci.py
       exactly according to that methodology.

9. If the exact algorithm/reference data is NOT available:
       DO NOT invent or approximate UTCI.
       Stop the UTCI implementation and clearly report what is missing.

10. When the algorithm is available:
       create tests/scientific_validation/test_utci.py
       using independent authoritative reference values.

11. Validate:
       temperature
       relative humidity
       wind speed
       mean radiant temperature
       -> UTCI

12. Only after UTCI passes its reference tests:
       connect UTCI -> existing Hazard normalization.

13. Add an integration test:
       meteorological inputs
       -> UTCI
       -> H

14. Run:
       pytest -q
       ruff check .
       mypy ...

Do not claim scientific validity merely because tests pass.

Final status must explicitly say:

    NORMALIZED RISK ENGINE READY
    or
    COMPLETE SCIENTIFIC ENGINE READY
    or
    UTCI IMPLEMENTATION BLOCKED — REFERENCE METHODOLOGY MISSING

Do not proceed to mortality ML.