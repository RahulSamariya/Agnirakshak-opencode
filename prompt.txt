DATA LAYER FINALIZATION — REAL FILES MUST BE INSIDE THE REPOSITORY

Project: Agnirakshak — MoES Problem Statement 26083

GOAL:
Finish ONLY the real Ahmedabad data-layer milestone.

Do NOT move to MRT, UTCI, H, V/E aggregation, HSRI, mortality ML,
hospitalization ML, or DeepTherm.

==================================================
1. INSPECT THE CURRENT REPOSITORY
==================================================

First inspect the actual repository and report:

- current working directory
- repo root
- existing data/
- existing docs/data/
- existing profiling scripts
- existing tests
- existing TODOs affecting this milestone

Do not assume previous work is present.

==================================================
2. FIND THE REAL PILOT FILES
==================================================

Search the repository/workspace for these actual files:

CENSUS:
    DDW_PCA2407_2011_MDDS with UI (1).xlsx

WEATHER:
    data_0.nc

GIS:
    wards_ahmedabad.geojson
    wards_ahmedabad.kml
    wards_ahmedabad.parquet
    wards_ahmedabad.pmtiles
    wards_ahmedabad.shp.zip

AQI:
    Ahmedabad city-level AQI Excel files
    January 2025
    February 2025
    March 2025
    April 2025
    May 2025

Also search for any duplicate copies.

PRINT THE EXACT PATH OF EVERY FILE FOUND.

==================================================
3. REAL DATA MUST BE COPIED INTO data/raw
==================================================

Create:

data/
├── raw/
│   ├── census/
│   ├── weather/
│   ├── gis/
│   └── aqi/
├── staging/
├── curated/
├── metadata/
└── profiles/

Copy the REAL source files byte-for-byte into:

data/raw/census/
data/raw/weather/
data/raw/gis/
data/raw/aqi/

Do NOT modify originals.

Do NOT create placeholder files.

Do NOT create synthetic replacements.

If a required file cannot be found:
    STOP that dataset
    report the exact missing file
    do not fabricate it

==================================================
4. VERIFY ACTUAL FILE ACCESS
==================================================

After copying, run real filesystem checks.

Verify that every required file physically exists under data/raw.

Run something equivalent to:

    Get-ChildItem data/raw -Recurse

and individual existence checks.

Do NOT say the task is complete unless these files are actually there.

==================================================
5. CHECKSUM + PROVENANCE
==================================================

For every raw file calculate SHA-256.

Create/update:

    data/metadata/acquisition-manifest.yaml

Each record must contain:

    dataset_id
    domain
    source_file
    source_sha256
    original_filename
    source_url
    acquired_at
    original_format
    evidence_type
    source_status
    access_status
    ml_suitability
    notes

Evidence types:

    PRIMARY_OFFICIAL
    OFFICIAL_DOCUMENTATION
    SECONDARY_RESEARCH
    LLM_ONLY

RULE:

    LLM_ONLY -> NEVER IMPLEMENT

==================================================
6. CENSUS PROFILING
==================================================

Use the REAL XLSX in data/raw/census/.

Inspect:

    workbook
    sheets
    rows
    columns
    geography fields
    ward ID
    ward name
    municipal corporation
    population
    age fields
    sex
    literacy
    workers
    non-workers
    household fields

Identify exactly the Ahmedabad Municipal Corporation records.

Confirm the actual AMC ward count.

Check:

    duplicate IDs
    missing IDs
    duplicate names
    missing values
    invalid numeric values

Create:

    data/profiles/census_ahmedabad_2011.json
    data/profiles/census_ahmedabad_2011.md

Also create a normalized staging output.

DO NOT calculate V_ward.

DO NOT calculate E_ward.

==================================================
7. ERA5-LAND PROFILING
==================================================

Use:

    data/raw/weather/data_0.nc

Read the ACTUAL NetCDF metadata.

Report:

    dimensions
    variables
    units
    latitude
    longitude
    time
    time range
    timestamp count
    frequency
    missing values
    geographic extent

Explicitly inspect for:

    t2m
    d2m
    u10
    v10
    sp
    ssrd
    strd

Only report variables that actually exist.

Create:

    data/profiles/era5land_ahmedabad.json
    data/profiles/era5land_ahmedabad.md

DO NOT:

    derive RH
    derive MRT
    calculate UTCI
    calculate H

That is a later milestone.

==================================================
8. GIS PROFILING
==================================================

Use the actual GIS files.

Inspect:

    feature count
    ward IDs
    ward names
    LGD identifiers
    CRS
    coordinate order
    geometry type
    geometry validity
    duplicate IDs
    empty geometries
    bounding box

IMPORTANT:

Verify coordinate axis order from the actual coordinates.

If the source uses an incorrect axis order, DO NOT overwrite raw files.

Create a derived normalized file:

    data/staging/gis/wards_ahmedabad_epsg4326_normalized.geojson

Record:

    original file
    original hash
    original CRS
    original coordinate order
    corrected CRS
    corrected coordinate order
    transformation
    transformation version

==================================================
9. CENSUS VS GIS — DO NOT FORCE JOIN
==================================================

Preserve this fact:

    Census 2011 = 57 AMC wards
    Current GIS = 48 wards

Do NOT:

    force IDs
    rename IDs
    directly join
    invent crosswalk
    silently aggregate

Create:

    docs/data/ahmedabad-spatial-reconciliation-v2.md

Document:

    ward counts
    IDs
    names
    LGD codes
    overlaps
    unmatched records
    known geography/version differences

If no authoritative crosswalk exists:

    status = CROSSWALK_REQUIRED

==================================================
10. ERA5 GRID VS CURRENT GIS
==================================================

Using the NORMALIZED GIS:

Test the actual ERA5 grid against current ward polygons.

Report:

    total ERA5 cells
    cells intersecting Ahmedabad
    cells intersecting wards
    number of wards hit
    wards receiving no direct cell
    cells intersecting multiple wards

Do NOT interpolate.

Do NOT calculate HSRI.

Create:

    docs/data/ahmedabad-era5-gis-compatibility-v1.md

==================================================
11. AQI PROFILING
==================================================

Use the actual Ahmedabad city-level AQI files in data/raw/aqi/.

Profile January–May 2025.

Determine:

    rows
    columns
    date coverage
    hourly structure
    observed values
    missing values
    duplicates
    AQI min
    AQI max
    timestamp format
    city/station metadata
    pollutant fields if present
    units if present

Do NOT assume station-level if the file is city-level.

Do NOT create ward AQI.

Do NOT impute AQI.

Create:

    data/profiles/aqi_ahmedabad_2025_01_05.json
    data/profiles/aqi_ahmedabad_2025_01_05.md

==================================================
12. TEMPORAL AVAILABILITY
==================================================

Create:

    docs/data/ahmedabad-temporal-availability-v2.md

Compare:

    Census
    ERA5-Land
    AQI
    GIS
    IMDAA
    mortality
    hospitalization

For each record:

    start
    end
    frequency
    timezone
    geography
    access status

Clearly identify:

    overlap
    non-overlap
    unknown

DO NOT manufacture a common training period.

==================================================
13. DATA QUALITY REPORT
==================================================

Create:

    docs/data/ahmedabad-data-quality-v1.md

For each dataset report:

    missingness
    duplicates
    invalid values
    timestamp anomalies
    identifier anomalies
    unit anomalies
    geometry anomalies

Use:

    PASS
    WARNING
    FAIL
    UNKNOWN

Do not silently repair raw data.

==================================================
14. PILOT PROFILE
==================================================

Create:

    docs/data/ahmedabad-pilot-profile-v2.md

Include:

| Dataset | Coverage | Spatial | Temporal | Quality | Status | Limitation |
|---|---|---|---|---|---|---|

Datasets:

    Census
    GIS
    ERA5-Land
    AQI
    IMDAA
    Mortality
    Hospitalization

==================================================
15. PILOT READINESS
==================================================

Create:

    docs/data/ahmedabad-pilot-readiness-v2.md

For each:

    Weather
    Population
    GIS
    AQI
    Mortality
    Hospitalization

Use only:

    READY
    PARTIAL
    BLOCKED
    UNKNOWN

Do NOT call mortality or hospitalization READY without actual target data.

==================================================
16. REQUIRED DATA CONTRACT FILES
==================================================

Ensure these physically exist:

docs/data/
├── india-data-contract-v1.md
├── source-register.md
├── data-dictionary.md
├── spatial-strategy.md
└── unresolved-data-gaps.md

If missing, create them based only on verified project decisions.

Do not invent source claims.

==================================================
17. SCIENTIFIC ENGINE PROTECTION
==================================================

DO NOT MODIFY:

    UTCI
    Hazard H
    Vulnerability V
    Exposure E
    HSRI = H × V × E

Do not modify scientific tests.

Do not introduce new scientific formulas.

==================================================
18. HEALTH DATA PROTECTION
==================================================

DO NOT:

    fabricate mortality
    fabricate hospitalization
    create synthetic targets
    turn monthly/annual values into daily targets
    infer missing deaths

Health targets remain BLOCKED unless real data are obtained.

==================================================
19. NO ML
==================================================

Do NOT:

    implement DeepTherm
    train Transformer
    train Random Forest
    train XGBoost
    create mortality_training.parquet
    create hospitalization_training.parquet
    construct 14-day ML sequences

==================================================
20. TESTS
==================================================

Run:

    pytest -q

    ruff check .

If configured:

    mypy ...

Do not weaken existing tests.

Add only data-layer tests required for this milestone.

==================================================
21. HARD COMPLETION GATE
==================================================

Before reporting COMPLETE, verify all required repository artifacts.

Run:

    Get-ChildItem data/raw -Recurse
    Get-ChildItem data/profiles -Recurse
    Get-ChildItem data/metadata -Recurse
    Get-ChildItem docs/data -Recurse

Also verify every required file with Test-Path.

The milestone is COMPLETE only when:

1. REAL Census XLSX is under data/raw/census/
2. REAL ERA5 NetCDF is under data/raw/weather/
3. REAL GIS files are under data/raw/gis/
4. REAL AQI files are under data/raw/aqi/
5. Raw files are untouched
6. Checksums/provenance exist
7. All required profiles exist
8. Spatial reconciliation report exists
9. Temporal report exists
10. Data-quality report exists
11. Pilot-readiness report exists
12. Required Data Contract files exist
13. Tests pass
14. No scientific engine changes occurred
15. No ML code was started
16. No synthetic health target was created

If ANY condition fails:

    REAL DATA LAYER RECONCILIATION INCOMPLETE

==================================================
22. FINAL RESPONSE FORMAT
==================================================

Return:

# REAL DATA LAYER RECONCILIATION

## Repository inspected
Summarize actual repository state.

## Files discovered
List exact source paths.

## Files copied
List exact data/raw paths.

## Census
Actual findings.

## ERA5-Land
Actual findings.

## GIS
Actual findings.

## AQI
Actual findings.

## Spatial compatibility
Actual findings.

## Temporal compatibility
Actual findings.

## Data quality
Actual findings.

## Provenance
Actual checksums/metadata.

## Tests
Exact commands and results.

## Files created
Exact paths.

## Blockers
Actual remaining blockers.

## Final status
Use exactly one:

    REAL DATA LAYER RECONCILIATION COMPLETE

or:

    REAL DATA LAYER RECONCILIATION INCOMPLETE

DO NOT claim completion from documentation alone.

DO NOT claim data exists if it was not physically found.

DO NOT invent missing files.

START NOW BY INSPECTING THE REPOSITORY AND LOCATING THE REAL FILES.